// importing react library
// importing reactdom to gain access to virtual DOM
// import in app component
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// render app component
ReactDOM.render(
    <App />,
  document.getElementById('root')
);
